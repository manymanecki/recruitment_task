package io.getint.recruitment_task;

import io.getint.recruitment_task.api.JiraSynchronizer;
import org.junit.Test;

public class JiraSynchronizerTests {
    @Test
    public void shouldSyncTasks() throws Exception {
        // Sorry, really wanted to include some tests but it would require me to expose senstive data.
        // I will be more than happy to showcase this application during our next interview;)
        JiraSynchronizer.main(null);
    }
}
